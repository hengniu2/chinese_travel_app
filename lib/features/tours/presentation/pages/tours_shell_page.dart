import 'package:flutter/material.dart';

/// Tours 功能壳页（占位，无业务逻辑）
class ToursShellPage extends StatelessWidget {
  const ToursShellPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('行程/跟团游')),
    );
  }
}
